name: Test dep scripts

on:
  pull_request:
    paths:
      - automation/update_deps.py
      - automation/dep_diff.py
      - automation/tests/**

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install pytest
        run: pip install pytest
      - name: Run tests
        run: pytest automation/tests/ -v
